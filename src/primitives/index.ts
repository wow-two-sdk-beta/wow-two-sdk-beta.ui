// L2 — headless primitives. Behavior + a11y, no visual styling beyond layout.
// Foundation layer; cannot import upward (enforced by ESLint boundaries).
//
// Sub-exports added as primitives ship; barrel kept tidy.

export {};
