// color — domain module. Pure color value-types + operations (no React, no upward deps).

export { GradientType } from './GradientType';
export { Gradient } from './Gradient';
export type {
  GradientStop,
  LinearGradient,
  RadialGradient,
} from './Gradient';
