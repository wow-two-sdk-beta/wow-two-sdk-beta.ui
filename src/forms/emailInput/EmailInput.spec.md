# EmailInput

## Purpose
`<input type="email">` with `autocomplete="email"`, email `inputmode`, spellcheck disabled.

## Dependencies
Foundation: `utils/cn`, `primitives/formControlContext`. Same-domain: `forms/InputStyles`.
