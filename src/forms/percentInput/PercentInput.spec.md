# PercentInput

## Purpose
`NumberInput` with a trailing `%` decoration. Value is the bare number; interpret 0–100 in your form layer.

## Props
Same as `NumberInput`.

## Dependencies
Foundation: `utils/cn`. Same-domain: `NumberInput`.
