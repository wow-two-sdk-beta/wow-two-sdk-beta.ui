# SegmentedControl

## Purpose
iOS-style connected pill row — visual variant of `ToggleButtonGroup`. Best for small mutually-exclusive option pickers (Day / Week / Month).

## Props
Same as `ToggleButtonGroup`.

## Dependencies
Foundation: `utils/cn`. Same-domain: `ToggleButtonGroup` (which uses `ToggleButton`).
