# Text

## Purpose
Body-text atom with `size`, `weight`, `color`, `align`, `isTruncated`, `isTabular` variants. Default tag `p`; pass `as` to switch.

## Dependencies
Foundation: `utils/cn`, `tailwind-variants`.
