# VStack

## Purpose
`Stack` preset with `direction="column"` — matches `Stack`'s default but provided for symmetry with `HStack`. Same props minus `direction`.

## Dependencies
Foundation: `utils/cn`. Same-domain: `Stack`.
