# FormHelperText

## Purpose
Hint copy under a form control. Sets `id={helperId}` from `FormControl` for `aria-describedby` wiring.

## Dependencies
Foundation: `utils/cn`, `primitives/formControlContext`.
