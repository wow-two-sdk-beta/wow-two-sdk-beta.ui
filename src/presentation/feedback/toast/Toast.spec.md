# Toast

## Purpose
Slotted toast — visual only. Pair: `ToastSimple` (atomic visual) + `Toast` (slotted) + `Toaster` (queue manager, L5).

## Props
Same shape as `Alert`/`Banner` plus `ToastSimple`'s severity.

## Dependencies
Foundation: `utils/cn`, `icons/Icon`. Same-domain: `ToastSimple`.
