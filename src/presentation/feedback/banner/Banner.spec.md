# Banner

## Purpose
Slotted full-width banner — Icon + Title + Description + Actions + close on top of `BannerSimple`. Pair: `BannerSimple` (atomic) + `Banner` (molecule).

## Props
Same shape as `Alert` plus `BannerSimple`'s severity.

## Dependencies
Foundation: `utils/cn`, `icons/Icon`. Same-domain: `BannerSimple`.
