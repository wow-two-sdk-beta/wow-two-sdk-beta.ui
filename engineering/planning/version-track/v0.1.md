# v0.1 — SDK organize

*Last updated: 2026-07-13*

First SDK version-track doc — **unsynced** from the npm `0.0.x` auto-bump (multiple chats push; version here tracks our iterations, not the package number). Scope: reshape the repo + consolidate docs/vectors.

## Iterations (worked one-by-one — each is big)

- **iter 1 — repo restructure** — **DONE (2026-07-13)**: whole package → `engineering/codebase/wow-two-front-beta-sdk/`; `src/` source-only + `tests/{unit,stories}` split via the `@src/*` alias (363 files relocated, 571 imports rewritten by codemod); `engineering/` = planning+architecture at root; CI `working-directory` + `package.json` `repository.directory` + `.gitignore` negation + `wow-two-ws/scripts/active.sh` re-pathed. Applied staged — **A** (relocate, 1813 green) then **B** (test/story extraction, 1813 green) — verify-green between. Prereqs cleared (taxonomy restructure landed + `sdk-structure.md` reviewed). Slug **`wow-two-front-beta-sdk`**; npm pkg `@wow-two-beta/ui` renamed later. Polish `p0.1` stub still pending.
- **iter 2 — docs hygiene** — **relocation DONE (2026-07-13)**: `docs/` → `engineering/{architecture,planning}` per `sdk-structure.md` (`architecture/` = architecture·decisions·testing·theming·common-standards·`analysis/`·`templates/`; `planning/` = planning.md + version-track); lead docs `engineering.md` + `codebase/codebase.md` added; all in-repo doc links rewired (CLAUDE·README·config comments·planning hub·external depth +1); build/lint green. **Content-audit REMAINING** (deferred — big + cross-repo, overlaps iter 6/7): delete stale `enum-alignment` record (campaign complete 2026-07-07 — first confirm its 16-enum shared registry is captured in `conventions/.../enums.md`) · split each vector-analysis (shipped facts → conventions, forward-looking stays); prose `docs/` mentions inside analysis docs re-path during that split.
- **iter 3 — component index + docs convention** — **DONE (2026-07-13)**: [`engineering/architecture/component-catalog.md`](../../architecture/component-catalog.md) — 248 entries (231 presentation across 7 groups + 17 foundation primitives), per-group with import subpath + one-line purpose each. **Derived** from the component folders by a committed generator (`engineering/architecture/gen-catalog.mjs`; `## Purpose` → hand-authored `OVERRIDES` → prose fallback) → the docs convention = regenerate-not-hand-edit. Registered in `CLAUDE.md` · `engineering.md` · planning Components row. **Deferred** (later iter): codify the derived-catalog pattern as a reusable convention in `sdk-structure.md` (the instance stays local/auto-gen).
- **iter 4 — vector registry** — **DONE (2026-07-13)**: [`engineering/architecture/vector-registry.md`](../../architecture/vector-registry.md) — master index: 15 shipped modules (subpaths) + 25 cross-cutting vectors (`targets.md` §2) + 3-wave infra pipeline (`frontend-modules.md`) + browser-API groups (§3) + possible-unbuilt. Indexes the deep docs, doesn't duplicate. `targets.md` **refreshed** (bounded, not rebuilt): §2 shipped-modules note + registry pointer · §3.6 Web-Storage → DONE (`foundation/storage`) · §3.8 http-client-shipped note (§9 lock revised in practice). Registered in `CLAUDE.md` · `engineering.md` · planning Vectors.
- **iter 5 — foundation data contracts** — **DONE (2026-07-13)**: shipped `foundation/http/Page.ts` (9 `IHas*` composables + `Page<T>` / `TokenPage<T,TToken>` / `CursorPage` alias) + `foundation/identifiers/Guid.ts` (branded `Guid` — `createV7`/`createV4`/`empty`/`parse`/`tryParse`/`isGuid`/`equals`/`compare`/`version`; own ~25-LOC v7, subpath `@wow-two-beta/ui/foundation/identifiers`). Wired barrel · tsup entry · package export · vitest include; verified green (build/typecheck/lint/suite). Design (both analyses done):
  - **pagination** — `IHas*` **generic** composables (`IHasPageToken<TToken>`, `IHasPageSize`, `IHasTotalCount`, `IHasQueryTiming`) → `Page<T>` / `TokenPage<T>` at `foundation/http/Page.ts` · `docs/analysis/pagination-model.md`
  - **Guid** — branded-string `Guid` matching `System.Guid` (`createV7`, `createV4` = `NewGuid`, `empty`/`parse`/`compare`; own ~25-LOC impl) at `foundation/identifiers/Guid.ts` · `docs/analysis/guid-type.md`
- **iter 6 — fold forms → conventions** — **DONE (2026-07-13)**: `wow-two-ws/conventions/development/frontend/presentation/forms.md` gained an **Entities** quick-reference (the forms-engine surface — `useAppForm`/`form.Field`/`form.Subscribe`/`useFieldArray`/`StandardSchemaV1`/`{Model}Schema`/`*ApiRequest`…) + a **Controls** component index (all 74 `presentation/forms` controls by family, with `*Field` chrome-bundle · Temporal · `Wizard` notes). Snippets + wiring rules were already folded (F-2). Cross-repo edit; stayed clear of the conventions lane's 2 in-flight files (`architecture.md`, `models.md`).
- **iter 7 — analysis → tasks** — **DONE (2026-07-13)**: added a **Backlog — trigger-gated / unscheduled** section to `planning.md` consolidating the forward-looking items that lived only in analyses — module-deferred (`errors`/`AppDevtools`/`uploadQueue`), forms deferred (union-error ergonomics item 1 @ 2nd-product · `fieldErrors` duck-type item 3a opportunistic · 5 completeness defers), global-state → iter 8, + the v0.1-meta debts (2b content-audit · catalog-convention) — each with trigger + analysis link. Active/queued (W2-b/c · S-1 · W3 · A11y-2 · P6) were already rows.
- **iter 8 — global-state swappable module** (owner-approved 2026-07-13): house contract + `zustand` (default) / `redux` adapter as **optional peers**, forms-engine-style — one shared conformance suite, app pins one adapter. Overrides the *bundle-a-state-manager* SKIP (ecosystem E#26) on the **swappable-abstraction** framing. Design pass first (contract + which app state actually needs it beyond RQ cache + `usePersistentState`), then build. Sizes as a wave item, not a small iter.

## Prerequisites (not SDK-local — `wow-two-ws`)

- **conventions taxonomy restructure** — **DONE**: `conventions/development/repo/structure/` (repo + sdk) · `repo/version-control/` · `deployment/hosting/` landed; `sdk-structure.md` is the reviewed, now-applied spec. No longer gating.

## Decisions

- version unsynced from npm — this doc's `v0.1` ≠ `package.json` `0.0.x`
- publish is allowlist-based (`files: dist · README · LICENSE`) → `engineering/`, `docs/`, `src/`, every `.md` never ship, wherever they live
- (pending) fork A + fork B — see O-1
