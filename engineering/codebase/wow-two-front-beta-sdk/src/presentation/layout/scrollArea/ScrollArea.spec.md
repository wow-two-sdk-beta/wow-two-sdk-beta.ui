# ScrollArea

## Purpose
Native-scrollbar container. `axis` controls which direction(s) overflow. Custom-styled scrollbar variant lives at L5.

## Props
| Name | Type | Default |
|---|---|---|
| `axis` | `'vertical' \| 'horizontal' \| 'both'` | `'vertical'` |

## Dependencies
Foundation: `utils/cn`.
