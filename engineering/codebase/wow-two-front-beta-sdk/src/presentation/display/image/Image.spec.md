# Image

## Purpose
`<img>` wrapper with error fallback. Default `alt=""` so consumers must pass it explicitly when meaningful.

## Props
| Name | Type | Default |
|---|---|---|
| `fallback` | `ReactNode` | — |
| `...rest` | `ImgHTMLAttributes` | — |

## Dependencies
Foundation: `utils/cn`.
