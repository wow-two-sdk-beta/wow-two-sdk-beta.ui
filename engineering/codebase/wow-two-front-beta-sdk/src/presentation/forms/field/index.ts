export { Field, type FieldProps } from './Field';

/**
 * @deprecated `FormField` was renamed to `Field` (the generic labeled wrapper).
 * Alias kept for one release so consumers can migrate; removed next.
 */
export { Field as FormField, type FieldProps as FormFieldProps } from './Field';
