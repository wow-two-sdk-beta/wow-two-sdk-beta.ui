# FormErrorMessage

## Purpose
Error copy under a form control. Renders only when the surrounding `FormControl` is `isInvalid`. `role="alert"` for AT.

## Dependencies
Foundation: `utils/cn`, `primitives/formControlContext`.
