# Switch

## Purpose
Toggle switch — native checkbox with `role="switch"`, styled as track + thumb.

## Props
| Name | Type | Default |
|---|---|---|
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` |

## Dependencies
Foundation: `utils/cn`, `primitives/formControlContext`.
