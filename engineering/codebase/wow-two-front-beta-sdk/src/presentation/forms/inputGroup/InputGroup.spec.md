# InputGroup

## Purpose
Visually joins a row / column of inputs — collapses inner radii like `ButtonGroup` does for buttons.

## Props
| Name | Type | Default |
|---|---|---|
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` |

## Dependencies
Foundation: `utils/cn`.
