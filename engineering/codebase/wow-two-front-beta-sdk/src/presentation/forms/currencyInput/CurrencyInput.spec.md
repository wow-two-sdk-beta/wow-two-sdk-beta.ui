# CurrencyInput

## Purpose
`NumberInput` with a leading currency symbol decoration. Input value remains the bare number.

## Props
| Name | Type | Default |
|---|---|---|
| `symbol` | `string` | `'$'` |
| ...rest | `NumberInputProps` | — |

## Dependencies
Foundation: `utils/cn`. Same-domain: `NumberInput`.
