// emoji — domain module. The bundled emoji catalog the picker consumes: full base-emoji set,
// categorized, searchable. Pure types/ops (no React, no upward deps).

export { EmojiCategory } from './EmojiCategory';
export { EmojiCatalog } from './EmojiCatalog';
export type { EmojiCatalogEntry } from './EmojiCatalog';
