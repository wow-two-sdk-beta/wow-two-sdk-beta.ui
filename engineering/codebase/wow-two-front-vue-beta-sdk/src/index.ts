/* ---------------------------------------------------------------------------
 * @wow-two-beta/ui-vue — root barrel.
 *
 * Deliberately empty. The Vue port lands layer by layer (foundation → domain →
 * presentation); each layer adds its re-exports here as it ships, mirroring the
 * React package's `src/index.ts`. Subpath imports
 * (`@wow-two-beta/ui-vue/foundation/utils`, …) work independently of this file.
 * ------------------------------------------------------------------------- */

export {};
