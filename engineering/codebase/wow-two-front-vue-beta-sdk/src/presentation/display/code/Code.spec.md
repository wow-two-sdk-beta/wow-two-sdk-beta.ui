# Code

## Purpose
Inline (`<code>`) or block (`<code class="block">`) code styling. Syntax highlighting is a separate L5 organism.

## Props
| Name | Type | Default |
|---|---|---|
| `variant` | `'inline' \| 'block'` | `'inline'` |

## Dependencies
Foundation: `utils/cn`, `tailwind-variants`.
